'use strict';

describe('intruderCtrl', function () {

	beforeEach(module('BB8.intruder'));

	describe('blah', function () {
		it('should do something', function () {
			expect(true).toEqual(true);
		});
	});
});
